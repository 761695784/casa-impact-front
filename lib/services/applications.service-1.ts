// Fichier orphelin (2026-09-15) : une copie plus ancienne et incomplète de
// `lib/services/applications.service.ts` (probablement laissée par un
// conflit de synchronisation OneDrive/Windows — suffixe "-1" typique). Aucun
// fichier du projet ne l'importe (vérifié par recherche exhaustive sur les
// 316 fichiers .ts/.tsx du projet) et elle contenait la même erreur de
// build que l'originale (`RawApplication extends Application` invalide côté
// `documents`). Vidée ici pour ne plus faire échouer le build ; peut être
// supprimée du disque sans risque.
export {}
